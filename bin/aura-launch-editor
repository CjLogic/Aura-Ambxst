#!/bin/bash

aura-cmd-present "$EDITOR" || EDITOR=fresh

case "$EDITOR" in
fresh | nano | micro | hx | helix)
  exec aura-launch-tui "$EDITOR" "$@"
  ;;
*)
  exec setsid uwsm-app -- "$EDITOR" "$@"
  ;;
esac
